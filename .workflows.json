{
  "workflows": {
    "server": {
      "name": "Express Server",
      "run": "cd server && npx tsx index.ts"
    },
    "angular": {
      "name": "Angular Client",
      "run": "cd angular-client && npx ng serve --port 4200 --host 0.0.0.0 --disable-host-check"
    }
  }
}
